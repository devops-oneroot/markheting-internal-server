name: Daily API Request

on:
  schedule:
  # Runs every day at 3:15 PM IST (9:45 AM UTC)
 - cron: "45 9 * * *"


jobs:
  hit-api:
    runs-on: ubuntu-latest

    steps:
      - name: Make API request
        run: |
          curl -X GET "https://campdash.onrender.com/update-database"
